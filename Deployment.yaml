apiVersion: apps/v1
kind: Deployment
metadata:
  name: docker
  labels:
    app: myapp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: myapp
  template:
    metadata:
      labels:
        app: myapp
    spec:
      containers:
      - name: dockerdep-10-app1
        image: containercks.azurecr.io/myimage:1.0.0
        ports:
        - containerPort: 80
      imagePullSecrets:
       - name: secret